node_modules/
.DS_Store
*.log
# wayfinding maps y research local del repo (no se publican)
wayfinder/
research/
.scratch/
# Bun es el gestor del repo; un package-lock.json npm local nunca debe colarse
package-lock.json
